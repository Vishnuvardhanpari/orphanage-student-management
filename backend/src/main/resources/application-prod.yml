spring:
  datasource:
    # Supabase requires SSL. Override DB_SSL_MODE only if your provider documents otherwise.
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT:5432}/${DB_NAME}?sslmode=${DB_SSL_MODE:require}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
  endpoint:
    health:
      show-details: never

# Set OMS_CORS_ALLOWED_ORIGINS to the production frontend origin(s), comma-separated.
# Empty default denies browser cross-origin access until configured.
oms:
  cors:
    allowed-origins: ${OMS_CORS_ALLOWED_ORIGINS:}
  storage:
    type: gcs
    gcs:
      bucket: ${GCS_BUCKET_NAME}

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

logging:
  level:
    com.orphanage.oms: INFO
    root: WARN

server:
  error:
    include-message: never
